openapi: 3.1.0
info:
  title: Zhyvo API
  version: 0.1.0
  description: REST API for ephemeral event media rooms. All timestamps are UTC ISO 8601 values.
  license:
    name: Proprietary
    identifier: LicenseRef-Proprietary
servers:
  - url: /api/v1
    description: Same-origin API base path
tags:
  - name: Auth
    description: Anonymous identity and device-session lifecycle
  - name: Rooms
    description: Ephemeral room creation, access, and owner settings
  - name: Uploads
    description: Direct single and multipart object-storage uploads
  - name: Media
    description: Gallery, original downloads, and media deletion
  - name: Support
    description: Privacy-safe problem reports
  - name: Admin
    description: Telegram-allowlisted internal operations

paths:
  /auth/anonymous:
    post:
      operationId: createAnonymousSession
      tags: [Auth]
      summary: Create an anonymous identity and device session
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AnonymousAuthRequest'
      responses:
        '201':
          description: Session created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
        '422': {$ref: '#/components/responses/ValidationError'}
        '429': {$ref: '#/components/responses/RateLimited'}

  /auth/telegram:
    post:
      operationId: createTelegramSession
      tags: [Auth]
      summary: Validate Telegram Mini App initData and create a device session
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/TelegramAuthRequest'}
      responses:
        '201':
          description: Telegram identity upserted and session created
          content:
            application/json:
              schema: {$ref: '#/components/schemas/TokenResponse'}
        '401': {description: initData is invalid or expired}
        '429': {$ref: '#/components/responses/RateLimited'}
        '503': {description: Telegram authentication is not configured}

  /auth/telegram/config:
    get:
      operationId: getTelegramLoginConfig
      tags: [Auth]
      summary: Get public browser-login availability and Client ID
      security: []
      responses:
        '200':
          description: Telegram OIDC configuration
          content:
            application/json:
              schema:
                type: object
                required: [enabled, client_id]
                properties:
                  enabled: {type: boolean}
                  client_id: {type: string}

  /auth/telegram/oidc:
    post:
      operationId: linkBrowserIdentityToTelegram
      tags: [Auth]
      summary: Exchange an OIDC authorization code and upgrade the current anonymous identity
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/TelegramOIDCRequest'}
      responses:
        '200':
          description: Identity linked; rooms, memberships, uploads and ownership preserved
          content:
            application/json:
              schema: {$ref: '#/components/schemas/TokenResponse'}
        '400': {description: Authorization code, PKCE verifier, nonce or ID token is invalid}
        '409': {description: Current identity cannot be upgraded}
        '503': {description: Telegram browser login is not configured}

  /auth/telegram/link-challenges:
    post:
      operationId: createTelegramBrowserLinkChallenge
      tags: [Auth]
      summary: Create a five-minute single-use browser linking challenge
      responses:
        '201':
          description: Challenge created for the current anonymous identity
          content:
            application/json:
              schema: {$ref: '#/components/schemas/BrowserLinkChallenge'}
        '409': {description: Current identity cannot be linked}

  /auth/telegram/link-challenges/status:
    post:
      operationId: getTelegramBrowserLinkStatus
      tags: [Auth]
      summary: Poll a challenge bound to the current anonymous identity
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/BrowserLinkRequest'}
      responses:
        '200':
          description: Current challenge status
          content:
            application/json:
              schema: {$ref: '#/components/schemas/BrowserLinkChallenge'}
        '404': {description: Challenge not found}

  /auth/telegram/link-challenges/approve:
    post:
      operationId: approveTelegramBrowserLink
      tags: [Auth]
      summary: Approve a browser challenge from an authenticated Telegram Mini App
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/BrowserLinkRequest'}
      responses:
        '200': {description: Challenge approved}
        '409': {description: Challenge expired, denied or already used}

  /auth/telegram/link-challenges/exchange:
    post:
      operationId: exchangeTelegramBrowserLink
      tags: [Auth]
      summary: Consume an approved challenge and upgrade the anonymous browser identity
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/BrowserLinkRequest'}
      responses:
        '200':
          description: Browser identity linked to Telegram
          content:
            application/json:
              schema: {$ref: '#/components/schemas/TokenResponse'}
        '409': {description: Challenge is not approved or is no longer available}

  /auth/refresh:
    post:
      operationId: refreshSession
      tags: [Auth]
      summary: Rotate a refresh token and issue a new access token
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [refresh_token]
              properties:
                refresh_token: {type: string, minLength: 32}
      responses:
        '200':
          description: Token rotated
          content:
            application/json:
              schema: {$ref: '#/components/schemas/TokenResponse'}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '429': {$ref: '#/components/responses/RateLimited'}

  /auth/me:
    get:
      operationId: getCurrentIdentity
      tags: [Auth]
      summary: Get the current identity
      responses:
        '200':
          description: Current identity
          content:
            application/json:
              schema:
                type: object
                required: [identity]
                properties:
                  identity: {$ref: '#/components/schemas/Identity'}
        '401': {$ref: '#/components/responses/Unauthorized'}

  /auth/session:
    delete:
      operationId: revokeCurrentSession
      tags: [Auth]
      summary: Revoke the current device session
      responses:
        '204': {description: Session revoked}
        '401': {$ref: '#/components/responses/Unauthorized'}

  /problem-reports:
    post:
      operationId: createProblemReport
      tags: [Support]
      summary: Submit a rate-limited problem report with optional technical context
      security: [{}, {bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [category, description, technical_context]
              properties:
                category: {type: string, enum: [upload, download, room, telegram, other]}
                description: {type: string, minLength: 10, maxLength: 2000}
                contact: {type: string, maxLength: 160}
                technical_context: {$ref: '#/components/schemas/ProblemReportContext'}
      responses:
        '201':
          description: Report saved and assigned a public reference
          content:
            application/json:
              schema:
                type: object
                required: [report]
                properties:
                  report: {$ref: '#/components/schemas/ProblemReport'}
        '422': {$ref: '#/components/responses/ValidationError'}
        '429': {$ref: '#/components/responses/RateLimited'}

  /admin/stats:
    get:
      operationId: getAdminStats
      tags: [Admin]
      summary: Get current aggregate product metrics
      responses:
        '200':
          description: Current PostgreSQL-backed product totals
          content:
            application/json:
              schema: {$ref: '#/components/schemas/AdminStats'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /admin/problem-reports:
    get:
      operationId: listProblemReports
      tags: [Admin]
      summary: List up to 100 reports for a Telegram-allowlisted administrator
      parameters:
        - name: status
          in: query
          schema: {type: string, enum: [new, in_progress, resolved, closed]}
        - name: category
          in: query
          schema: {type: string, enum: [upload, download, room, telegram, other]}
      responses:
        '200':
          description: Report queue
          content:
            application/json:
              schema:
                type: object
                required: [reports]
                properties:
                  reports: {type: array, items: {$ref: '#/components/schemas/ProblemReport'}}
        '403': {$ref: '#/components/responses/Forbidden'}

  /admin/problem-reports/{reportID}:
    parameters:
      - name: reportID
        in: path
        required: true
        schema: {type: string, format: uuid}
    get:
      operationId: getProblemReport
      tags: [Admin]
      summary: Get one report
      responses:
        '200':
          description: Report details
          content:
            application/json:
              schema:
                type: object
                required: [report]
                properties:
                  report: {$ref: '#/components/schemas/ProblemReport'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
    patch:
      operationId: updateProblemReport
      tags: [Admin]
      summary: Change report status and internal note
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [status, admin_note]
              properties:
                status: {type: string, enum: [new, in_progress, resolved, closed]}
                admin_note: {type: string, maxLength: 2000}
      responses:
        '200':
          description: Updated report
          content:
            application/json:
              schema:
                type: object
                required: [report]
                properties:
                  report: {$ref: '#/components/schemas/ProblemReport'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /rooms:
    get:
      operationId: listMyRooms
      tags: [Rooms]
      summary: List active rooms joined by the current identity
      responses:
        '200':
          description: Active rooms ordered by recent membership activity
          content:
            application/json:
              schema:
                type: object
                required: [rooms]
                properties:
                  rooms:
                    type: array
                    items: {$ref: '#/components/schemas/Room'}
        '401': {$ref: '#/components/responses/Unauthorized'}
    post:
      operationId: createRoom
      tags: [Rooms]
      summary: Create a room
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/CreateRoomRequest'}
      responses:
        '201':
          description: Room created, or the original result of an idempotent retry
          content:
            application/json:
              schema:
                type: object
                required: [room, share_path]
                properties:
                  room: {$ref: '#/components/schemas/Room'}
                  share_path: {type: string, example: /r/K7MP2Q}
        '401': {$ref: '#/components/responses/Unauthorized'}
        '409': {$ref: '#/components/responses/Conflict'}
        '422': {$ref: '#/components/responses/ValidationError'}

  /rooms/{slug}/preview:
    get:
      operationId: getRoomPreview
      tags: [Rooms]
      summary: Get public room information before joining
      security: []
      parameters:
        - $ref: '#/components/parameters/RoomSlug'
      responses:
        '200':
          description: Room preview
          content:
            application/json:
              schema: {$ref: '#/components/schemas/RoomPreview'}
        '404': {$ref: '#/components/responses/NotFound'}
        '410': {$ref: '#/components/responses/Gone'}

  /rooms/{slug}/join:
    post:
      operationId: joinRoom
      tags: [Rooms]
      summary: Join a public or protected room
      parameters:
        - $ref: '#/components/parameters/RoomSlug'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                secret: {type: string, description: PIN or password; omit for public rooms}
      responses:
        '200':
          description: Room membership created or refreshed
          content:
            application/json:
              schema:
                type: object
                required: [room]
                properties:
                  room: {$ref: '#/components/schemas/Room'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '410': {$ref: '#/components/responses/Gone'}
        '429': {$ref: '#/components/responses/RateLimited'}

  /invites/{token}/preview:
    get:
      operationId: getManagedInvitePreview
      tags: [Rooms]
      summary: Preview a revocable room invitation
      security: []
      parameters:
        - $ref: '#/components/parameters/InviteToken'
      responses:
        '200':
          description: Invitation and room preview
          content:
            application/json:
              schema: {$ref: '#/components/schemas/RoomInvitePreview'}
        '404': {$ref: '#/components/responses/NotFound'}
        '410': {$ref: '#/components/responses/Gone'}

  /invites/{token}/join:
    post:
      operationId: joinRoomByManagedInvite
      tags: [Rooms]
      summary: Join with the permission carried by a managed invitation
      parameters:
        - $ref: '#/components/parameters/InviteToken'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                secret: {type: string}
      responses:
        '200':
          description: Room membership created, retained, or upgraded
          content:
            application/json:
              schema:
                type: object
                required: [room]
                properties:
                  room: {$ref: '#/components/schemas/Room'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '410': {$ref: '#/components/responses/Gone'}

  /rooms/{slug}/invites:
    get:
      operationId: listManagedInvites
      tags: [Rooms]
      summary: List managed invitations as the room owner
      parameters:
        - $ref: '#/components/parameters/RoomSlug'
      responses:
        '200':
          description: Invitation management state
          content:
            application/json:
              schema: {$ref: '#/components/schemas/RoomInviteList'}
        '403': {$ref: '#/components/responses/Forbidden'}
    post:
      operationId: createManagedInvite
      tags: [Rooms]
      summary: Create a contributor or view-only invitation as the owner
      parameters:
        - $ref: '#/components/parameters/RoomSlug'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [permission]
              properties:
                permission: {type: string, enum: [contributor, viewer]}
      responses:
        '201':
          description: Invitation created
          content:
            application/json:
              schema: {$ref: '#/components/schemas/RoomInvite'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /rooms/{slug}/share-invite:
    get:
      operationId: getCurrentShareInvite
      tags: [Rooms]
      summary: Get the active contributor invitation as a room member
      parameters:
        - $ref: '#/components/parameters/RoomSlug'
      responses:
        '200':
          description: Current contributor invitation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/RoomInvite'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /rooms/{slug}/invites/{token}:
    delete:
      operationId: revokeManagedInvite
      tags: [Rooms]
      summary: Permanently revoke an invitation as the room owner
      parameters:
        - $ref: '#/components/parameters/RoomSlug'
        - $ref: '#/components/parameters/InviteToken'
      responses:
        '204': {description: Invitation revoked}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /rooms/{slug}/legacy-invite:
    delete:
      operationId: disableLegacyRoomInvite
      tags: [Rooms]
      summary: Permanently disable new joins through the room-code link
      parameters:
        - $ref: '#/components/parameters/RoomSlug'
      responses:
        '204': {description: Legacy link disabled}
        '403': {$ref: '#/components/responses/Forbidden'}

  /rooms/{slug}:
    parameters:
      - $ref: '#/components/parameters/RoomSlug'
    get:
      operationId: getRoom
      tags: [Rooms]
      summary: Get a room as a member
      responses:
        '200':
          description: Room details
          content:
            application/json:
              schema:
                type: object
                required: [room]
                properties:
                  room: {$ref: '#/components/schemas/Room'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '410': {$ref: '#/components/responses/Gone'}
    patch:
      operationId: updateRoom
      tags: [Rooms]
      summary: Update room settings as the owner
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/UpdateRoomRequest'}
      responses:
        '200':
          description: Updated room
          content:
            application/json:
              schema:
                type: object
                required: [room]
                properties:
                  room: {$ref: '#/components/schemas/Room'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '410': {$ref: '#/components/responses/Gone'}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      operationId: deleteRoom
      tags: [Rooms]
      summary: Schedule immediate room deletion as the owner
      responses:
        '202': {description: Room closed and scheduled for background deletion}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /rooms/{slug}/members:
    get:
      operationId: listRoomMembers
      tags: [Rooms]
      summary: List room participants as the owner
      parameters:
        - $ref: '#/components/parameters/RoomSlug'
      responses:
        '200':
          description: Room owner and joined participants
          content:
            application/json:
              schema:
                type: object
                required: [members, blocked_members]
                properties:
                  members:
                    type: array
                    items: {$ref: '#/components/schemas/RoomMember'}
                  blocked_members:
                    type: array
                    items: {$ref: '#/components/schemas/BlockedRoomMember'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '410': {$ref: '#/components/responses/Gone'}

  /rooms/{slug}/members/{identityID}:
    delete:
      operationId: removeRoomMember
      tags: [Rooms]
      summary: Remove and block a participant as the owner
      parameters:
        - $ref: '#/components/parameters/RoomSlug'
        - name: identityID
          in: path
          required: true
          schema: {type: string, format: uuid}
      responses:
        '204': {description: Participant removed and blocked}
        '400': {$ref: '#/components/responses/BadRequest'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /rooms/{slug}/blocked-members/{identityID}:
    delete:
      operationId: unblockRoomMember
      tags: [Rooms]
      summary: Allow a blocked participant to join again
      parameters:
        - $ref: '#/components/parameters/RoomSlug'
        - name: identityID
          in: path
          required: true
          schema: {type: string, format: uuid}
      responses:
        '204': {description: Participant unblocked}
        '400': {$ref: '#/components/responses/BadRequest'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /rooms/{slug}/ownership:
    post:
      operationId: transferRoomOwnership
      tags: [Rooms]
      summary: Transfer room ownership to an active participant
      parameters:
        - $ref: '#/components/parameters/RoomSlug'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [identity_id]
              properties:
                identity_id: {type: string, format: uuid}
      responses:
        '200':
          description: Ownership transferred; the caller is now a member
          content:
            application/json:
              schema:
                type: object
                required: [room]
                properties:
                  room: {$ref: '#/components/schemas/Room'}
        '400': {$ref: '#/components/responses/BadRequest'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /rooms/{slug}/activity:
    get:
      operationId: listRoomActivity
      tags: [Rooms]
      summary: List the latest room activity as the owner
      parameters:
        - $ref: '#/components/parameters/RoomSlug'
      responses:
        '200':
          description: Up to 100 latest events in reverse chronological order
          content:
            application/json:
              schema:
                type: object
                required: [events]
                properties:
                  events:
                    type: array
                    items: {$ref: '#/components/schemas/RoomActivityEvent'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /rooms/{slug}/recap:
    get:
      operationId: getRoomRecap
      tags: [Rooms]
      summary: Get full-room event totals as an active member
      parameters:
        - $ref: '#/components/parameters/RoomSlug'
      responses:
        '200':
          description: Event recap totals
          content:
            application/json:
              schema: {$ref: '#/components/schemas/RoomRecap'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /rooms/{slug}/highlights:
    get:
      operationId: getRoomHighlights
      tags: [Media]
      summary: Get up to twelve server-ranked highlights across the room
      parameters:
        - $ref: '#/components/parameters/RoomSlug'
      responses:
        '200':
          description: Gallery items ranked by favorites and recency
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    maxItems: 12
                    items: {$ref: '#/components/schemas/GalleryItem'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /rooms/{slug}/uploads:
    post:
      operationId: initiateUpload
      tags: [Uploads]
      summary: Reserve capacity and initialize a single or multipart upload
      parameters:
        - $ref: '#/components/parameters/RoomSlug'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/InitiateUploadRequest'}
      responses:
        '201':
          description: Upload initialized
          content:
            application/json:
              schema:
                type: object
                required: [upload, media_id]
                properties:
                  upload: {$ref: '#/components/schemas/Upload'}
                  media_id: {type: string, format: uuid}
        '409': {$ref: '#/components/responses/Conflict'}
        '422': {$ref: '#/components/responses/ValidationError'}
        '429': {$ref: '#/components/responses/RateLimited'}

  /uploads/{uploadID}/parts:
    post:
      operationId: getMultipartPartURLs
      tags: [Uploads]
      summary: Issue presigned URLs for a batch of multipart parts
      parameters:
        - $ref: '#/components/parameters/UploadID'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [part_numbers]
              properties:
                part_numbers:
                  type: array
                  minItems: 1
                  maxItems: 20
                  uniqueItems: true
                  items: {type: integer, minimum: 1}
      responses:
        '200':
          description: Presigned part URLs
          content:
            application/json:
              schema:
                type: object
                required: [parts]
                properties:
                  parts:
                    type: array
                    items: {$ref: '#/components/schemas/PartURL'}
        '410': {$ref: '#/components/responses/Gone'}
        '422': {$ref: '#/components/responses/ValidationError'}
        '429': {$ref: '#/components/responses/RateLimited'}

  /uploads/{uploadID}/complete:
    post:
      operationId: completeUpload
      tags: [Uploads]
      summary: Verify and complete an upload
      parameters:
        - $ref: '#/components/parameters/UploadID'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                parts:
                  type: array
                  description: Required for multipart; omit for a single upload
                  items: {$ref: '#/components/schemas/CompletedPart'}
      responses:
        '200':
          description: Media is ready and thumbnail processing is queued
          content:
            application/json:
              schema:
                type: object
                required: [media]
                properties:
                  media:
                    type: object
                    required: [id, status]
                    properties:
                      id: {type: string, format: uuid}
                      status: {type: string, const: ready}
        '409': {$ref: '#/components/responses/Conflict'}
        '410': {$ref: '#/components/responses/Gone'}
        '422': {$ref: '#/components/responses/ValidationError'}

  /uploads/{uploadID}:
    delete:
      operationId: abortUpload
      tags: [Uploads]
      summary: Abort an upload and release its room reservation
      parameters:
        - $ref: '#/components/parameters/UploadID'
      responses:
        '204': {description: Upload aborted or already inactive}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}

  /rooms/{slug}/media:
    get:
      operationId: getRoomGallery
      tags: [Media]
      summary: Get a cursor-paginated gallery
      parameters:
        - $ref: '#/components/parameters/RoomSlug'
        - name: limit
          in: query
          schema: {type: integer, minimum: 1, maximum: 100, default: 50}
        - name: cursor
          in: query
          schema: {type: string}
      responses:
        '200':
          description: Gallery page
          content:
            application/json:
              schema: {$ref: '#/components/schemas/GalleryPage'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}

  /rooms/{slug}/events:
    get:
      operationId: streamRoomEvents
      tags: [Rooms]
      summary: Stream durable room changes using Server-Sent Events
      description: |
        Sends an initial `ready` event followed by `room` events. Clients reconnect
        with `Last-Event-ID`; heartbeat comments keep idle proxies from closing the
        connection. The stream ends when room access is revoked or expires.
      parameters:
        - $ref: '#/components/parameters/RoomSlug'
        - name: Last-Event-ID
          in: header
          required: false
          schema: {type: integer, format: int64, minimum: 0}
      responses:
        '200':
          description: SSE stream of room events
          headers:
            Cache-Control:
              schema: {type: string, const: 'no-cache, no-transform'}
          content:
            text/event-stream:
              schema:
                type: string
                description: SSE frames whose room-event data matches RoomRealtimeEvent
        '403': {$ref: '#/components/responses/Forbidden'}
        '422': {$ref: '#/components/responses/ValidationError'}

  /media/{mediaID}/download-url:
    post:
      operationId: getMediaDownloadURL
      tags: [Media]
      summary: Issue a presigned original download URL
      parameters:
        - $ref: '#/components/parameters/MediaID'
      responses:
        '200':
          description: Short-lived download URL
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Download'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}

  /media/{mediaID}/favorite:
    put:
      operationId: addMediaFavorite
      tags: [Media]
      summary: Add the current member's favorite reaction
      parameters:
        - $ref: '#/components/parameters/MediaID'
      responses:
        '200':
          description: Updated favorite state
          content:
            application/json:
              schema: {$ref: '#/components/schemas/FavoriteState'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}
    delete:
      operationId: removeMediaFavorite
      tags: [Media]
      summary: Remove the current member's favorite reaction
      parameters:
        - $ref: '#/components/parameters/MediaID'
      responses:
        '200':
          description: Updated favorite state
          content:
            application/json:
              schema: {$ref: '#/components/schemas/FavoriteState'}
        '404': {$ref: '#/components/responses/NotFound'}

  /rooms/{slug}/cover:
    put:
      operationId: setRoomCover
      tags: [Rooms]
      summary: Set a ready image as the room cover (owner only)
      parameters:
        - $ref: '#/components/parameters/RoomSlug'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [media_id]
              properties:
                media_id: {type: string, format: uuid}
      responses:
        '204': {description: Room cover updated}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /media/{mediaID}:
    patch:
      operationId: updateMediaCaption
      tags: [Media]
      summary: Add, edit, or clear a media caption as its uploader or room owner
      parameters:
        - $ref: '#/components/parameters/MediaID'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [caption]
              properties:
                caption: {type: string, maxLength: 300, description: Whitespace is trimmed; an empty string clears the caption}
      responses:
        '200':
          description: Updated caption
          content:
            application/json:
              schema: {$ref: '#/components/schemas/CaptionState'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '422': {$ref: '#/components/responses/ValidationError'}
    delete:
      operationId: deleteMedia
      tags: [Media]
      summary: Delete media as its uploader or room owner
      parameters:
        - $ref: '#/components/parameters/MediaID'
      responses:
        '204': {description: Media deleted}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}

  /rooms/{slug}/archive:
    post:
      operationId: requestRoomArchive
      tags: [Media]
      summary: Create or reuse a ZIP archive for the current gallery revision
      parameters:
        - $ref: '#/components/parameters/RoomSlug'
      responses:
        '200':
          description: A matching archive is already ready
          content:
            application/json:
              schema: {$ref: '#/components/schemas/RoomArchiveResponse'}
        '202':
          description: Archive generation is queued or in progress
          content:
            application/json:
              schema: {$ref: '#/components/schemas/RoomArchiveResponse'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '409': {$ref: '#/components/responses/Conflict'}
        '410': {$ref: '#/components/responses/Gone'}

  /archives/{archiveID}:
    get:
      operationId: getRoomArchive
      tags: [Media]
      summary: Get ZIP generation status and progress
      parameters:
        - $ref: '#/components/parameters/ArchiveID'
      responses:
        '200':
          description: Archive status
          content:
            application/json:
              schema: {$ref: '#/components/schemas/RoomArchiveResponse'}
        '404': {$ref: '#/components/responses/NotFound'}

  /archives/{archiveID}/download-url:
    post:
      operationId: getRoomArchiveDownloadURL
      tags: [Media]
      summary: Issue a presigned URL for a ready ZIP archive
      parameters:
        - $ref: '#/components/parameters/ArchiveID'
      responses:
        '200':
          description: Short-lived ZIP download URL
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Download'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  parameters:
    RoomSlug:
      name: slug
      in: path
      required: true
      schema: {type: string, minLength: 6, maxLength: 12, pattern: '^[A-Z0-9]+$'}
    InviteToken:
      name: token
      in: path
      required: true
      schema: {type: string, minLength: 43, maxLength: 43}
    UploadID:
      name: uploadID
      in: path
      required: true
      schema: {type: string, format: uuid}
    MediaID:
      name: mediaID
      in: path
      required: true
      schema: {type: string, format: uuid}
    ArchiveID:
      name: archiveID
      in: path
      required: true
      schema: {type: string, format: uuid}
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      schema: {type: string, format: uuid}

  responses:
    BadRequest:
      description: Request path or JSON body is invalid
      content: {application/json: {schema: {$ref: '#/components/schemas/ErrorResponse'}}}
    Unauthorized:
      description: Authentication is missing, invalid, expired, or revoked
      content: {application/json: {schema: {$ref: '#/components/schemas/ErrorResponse'}}}
    Forbidden:
      description: The identity does not have permission
      content: {application/json: {schema: {$ref: '#/components/schemas/ErrorResponse'}}}
    NotFound:
      description: Resource not found
      content: {application/json: {schema: {$ref: '#/components/schemas/ErrorResponse'}}}
    Gone:
      description: Room or upload has expired
      content: {application/json: {schema: {$ref: '#/components/schemas/ErrorResponse'}}}
    Conflict:
      description: Current state or idempotency conflict
      content: {application/json: {schema: {$ref: '#/components/schemas/ErrorResponse'}}}
    ValidationError:
      description: Input validation failed
      content: {application/json: {schema: {$ref: '#/components/schemas/ErrorResponse'}}}
    RateLimited:
      description: Too many requests
      headers:
        Retry-After: {schema: {type: integer}, description: Suggested wait in seconds}
      content: {application/json: {schema: {$ref: '#/components/schemas/ErrorResponse'}}}

  schemas:
    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message, request_id]
          properties:
            code: {type: string, example: ROOM_ACCESS_DENIED}
            message: {type: string}
            request_id: {type: string}

    Identity:
      type: object
      required: [id, kind, display_name]
      properties:
        id: {type: string, format: uuid}
        kind: {type: string, enum: [anonymous, telegram, account]}
        display_name: {type: string, minLength: 1, maxLength: 80}
        telegram_user_id: {type: integer, format: int64, description: Present only for the authenticated user's own Telegram identity}

    AnonymousAuthRequest:
      type: object
      additionalProperties: false
      required: [display_name, client_type]
      properties:
        display_name: {type: string, minLength: 1, maxLength: 80}
        client_type: {type: string, enum: [web, ios, android]}

    TelegramAuthRequest:
      type: object
      additionalProperties: false
      required: [init_data]
      properties:
        init_data:
          type: string
          minLength: 1
          description: Raw, unmodified value from Telegram.WebApp.initData

    TelegramOIDCRequest:
      type: object
      additionalProperties: false
      required: [code, code_verifier, nonce]
      properties:
        code: {type: string, minLength: 1}
        code_verifier: {type: string, minLength: 43, maxLength: 128}
        nonce: {type: string, minLength: 1}

    BrowserLinkRequest:
      type: object
      additionalProperties: false
      required: [token]
      properties:
        token: {type: string, minLength: 43, maxLength: 43}

    BrowserLinkChallenge:
      type: object
      required: [status, expires_at]
      properties:
        token:
          type: string
          minLength: 43
          maxLength: 43
          description: Returned only when the challenge is created
        status: {type: string, enum: [pending, approved, consumed, denied, expired]}
        expires_at: {type: string, format: date-time}

    TokenResponse:
      type: object
      required: [identity, access_token, access_token_expires_in, access_token_expires_at, refresh_token, refresh_token_expires_at]
      properties:
        identity: {$ref: '#/components/schemas/Identity'}
        access_token: {type: string}
        access_token_expires_in: {type: integer, minimum: 0}
        access_token_expires_at: {type: string, format: date-time}
        refresh_token: {type: string}
        refresh_token_expires_at: {type: string, format: date-time}

    RoomAccess:
      oneOf:
        - type: object
          additionalProperties: false
          required: [mode]
          properties:
            mode: {type: string, const: public}
        - type: object
          additionalProperties: false
          required: [mode, secret]
          properties:
            mode: {type: string, const: pin}
            secret: {type: string, pattern: '^[0-9]{4,8}$'}
        - type: object
          additionalProperties: false
          required: [mode, secret]
          properties:
            mode: {type: string, const: password}
            secret: {type: string, minLength: 6, maxLength: 72}

    CreateRoomRequest:
      type: object
      additionalProperties: false
      required: [name, lifetime_days, access]
      properties:
        name: {type: string, minLength: 1, maxLength: 120}
        lifetime_days: {type: integer, enum: [1, 2, 3]}
        access: {$ref: '#/components/schemas/RoomAccess'}

    UpdateRoomRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        name: {type: string, minLength: 1, maxLength: 120}
        accepting_uploads: {type: boolean}
        accepting_members: {type: boolean}
        lifetime_days: {type: integer, enum: [1, 2, 3]}
        access: {$ref: '#/components/schemas/RoomAccess'}

    Room:
      type: object
      required: [id, slug, name, access_mode, role, can_upload, status, accepting_uploads, accepting_members, max_files, max_storage_bytes, used_files, used_storage_bytes, created_at, expires_at]
      properties:
        id: {type: string, format: uuid}
        slug: {type: string}
        name: {type: string}
        access_mode: {type: string, enum: [public, pin, password]}
        role: {type: string, enum: [owner, member]}
        can_upload: {type: boolean, description: Effective upload permission for the current identity}
        status: {type: string, enum: [active, deleting]}
        accepting_uploads: {type: boolean}
        accepting_members: {type: boolean}
        max_files: {type: integer}
        max_storage_bytes: {type: integer, format: int64}
        used_files: {type: integer}
        used_storage_bytes: {type: integer, format: int64}
        created_at: {type: string, format: date-time}
        expires_at: {type: string, format: date-time}

    RoomPreview:
      type: object
      required: [slug, name, access_mode, status, accepting_members, expires_at]
      properties:
        slug: {type: string}
        name: {type: string}
        access_mode: {type: string, enum: [public, pin, password]}
        status: {type: string}
        accepting_members: {type: boolean}
        expires_at: {type: string, format: date-time}

    RoomInvitePreview:
      allOf:
        - $ref: '#/components/schemas/RoomPreview'
        - type: object
          required: [permission]
          properties:
            permission: {type: string, enum: [contributor, viewer]}

    RoomInvite:
      type: object
      required: [token, permission, created_at, join_count]
      properties:
        token: {type: string, minLength: 43, maxLength: 43}
        permission: {type: string, enum: [contributor, viewer]}
        created_at: {type: string, format: date-time}
        revoked_at: {type: [string, 'null'], format: date-time}
        last_used_at: {type: [string, 'null'], format: date-time}
        join_count: {type: integer, minimum: 0}

    RoomInviteList:
      type: object
      required: [invites, legacy_invites_enabled]
      properties:
        invites:
          type: array
          items: {$ref: '#/components/schemas/RoomInvite'}
        legacy_invites_enabled: {type: boolean}

    RoomRecap:
      type: object
      required: [media_count, image_count, video_count, member_count, contributor_count, favorite_count, total_bytes, created_at, expires_at]
      properties:
        media_count: {type: integer, minimum: 0}
        image_count: {type: integer, minimum: 0}
        video_count: {type: integer, minimum: 0}
        member_count: {type: integer, minimum: 1}
        contributor_count: {type: integer, minimum: 0}
        favorite_count: {type: integer, minimum: 0}
        total_bytes: {type: integer, format: int64, minimum: 0}
        created_at: {type: string, format: date-time}
        expires_at: {type: string, format: date-time}

    RoomMember:
      type: object
      required: [id, display_name, role, can_upload, joined_at, last_seen_at]
      properties:
        id: {type: string, format: uuid}
        display_name: {type: string}
        role: {type: string, enum: [owner, member]}
        can_upload: {type: boolean}
        joined_at: {type: string, format: date-time}
        last_seen_at: {type: string, format: date-time}

    BlockedRoomMember:
      type: object
      required: [id, display_name, blocked_at]
      properties:
        id: {type: string, format: uuid}
        display_name: {type: string}
        blocked_at: {type: string, format: date-time}

    RoomActivityEvent:
      type: object
      required: [id, type, actor_id, actor_display_name, created_at]
      properties:
        id: {type: string, format: uuid}
        type:
          type: string
          enum: [room_created, member_joined, member_removed, member_unblocked, ownership_transferred, room_updated]
        actor_id: {type: string, format: uuid}
        actor_display_name: {type: string}
        subject_id: {type: string, format: uuid}
        subject_display_name: {type: string}
        created_at: {type: string, format: date-time}

    RoomArchive:
      type: object
      required: [id, room_slug, status, filename, total_files, processed_files, total_bytes, processed_bytes, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        room_slug: {type: string}
        status: {type: string, enum: [pending, processing, ready, failed]}
        filename: {type: string}
        total_files: {type: integer, minimum: 1}
        processed_files: {type: integer, minimum: 0}
        total_bytes: {type: integer, format: int64, minimum: 0}
        processed_bytes: {type: integer, format: int64, minimum: 0}
        size_bytes: {type: integer, format: int64, minimum: 0}
        error: {type: string}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
        completed_at: {type: string, format: date-time}

    RoomArchiveResponse:
      type: object
      required: [archive]
      properties:
        archive: {$ref: '#/components/schemas/RoomArchive'}

    InitiateUploadRequest:
      type: object
      additionalProperties: false
      required: [filename, mime_type, size_bytes]
      properties:
        filename: {type: string, minLength: 1, maxLength: 255}
        mime_type: {type: string}
        size_bytes: {type: integer, format: int64, minimum: 1, maximum: 2147483648}
        captured_at: {type: [string, 'null'], format: date-time}
        checksum:
          type: string
          pattern: '^[a-fA-F0-9]{64}$'
          description: Client-computed SHA-256 digest used to reject duplicate content before storage upload.

    Upload:
      type: object
      required: [id, media_id, type, status, expires_at]
      properties:
        id: {type: string, format: uuid}
        media_id: {type: string, format: uuid}
        type: {type: string, enum: [single, multipart]}
        status: {type: string, enum: [initiated, uploading, completed, aborted, expired]}
        part_size_bytes: {type: integer, format: int64}
        parts_count: {type: integer}
        expires_at: {type: string, format: date-time}
        url: {type: string, format: uri}
        url_expires_at: {type: string, format: date-time}
        method: {type: string, const: PUT}
        headers: {type: object, additionalProperties: {type: string}}

    PartURL:
      type: object
      required: [part_number, url, expires_at]
      properties:
        part_number: {type: integer, minimum: 1}
        url: {type: string, format: uri}
        expires_at: {type: string, format: date-time}

    CompletedPart:
      type: object
      additionalProperties: false
      required: [part_number, etag]
      properties:
        part_number: {type: integer, minimum: 1}
        etag: {type: string, minLength: 1, maxLength: 256}

    Uploader:
      type: object
      required: [id, display_name]
      properties:
        id: {type: string, format: uuid}
        display_name: {type: string}

    GalleryItem:
      type: object
      required: [id, media_type, mime_type, original_filename, size_bytes, created_at, thumbnail_url, thumbnail_status, uploaded_by, permissions, favorite_count, favorited, is_cover, caption, caption_updated_at]
      properties:
        id: {type: string, format: uuid}
        media_type: {type: string, enum: [image, video]}
        mime_type: {type: string}
        original_filename: {type: string}
        size_bytes: {type: integer, format: int64}
        width: {type: integer}
        height: {type: integer}
        duration_ms: {type: integer, format: int64}
        captured_at: {type: [string, 'null'], format: date-time}
        created_at: {type: string, format: date-time}
        thumbnail_url: {type: [string, 'null'], format: uri}
        thumbnail_status: {type: string, enum: [pending, processing, ready, failed]}
        uploaded_by: {$ref: '#/components/schemas/Uploader'}
        permissions:
          type: object
          required: [can_delete, can_edit_caption]
          properties:
            can_delete: {type: boolean}
            can_edit_caption: {type: boolean}
        favorite_count: {type: integer, minimum: 0}
        favorited: {type: boolean}
        is_cover: {type: boolean}
        caption: {type: [string, 'null'], maxLength: 300}
        caption_updated_at: {type: [string, 'null'], format: date-time}

    CaptionState:
      type: object
      required: [caption, caption_updated_at]
      properties:
        caption: {type: [string, 'null'], maxLength: 300}
        caption_updated_at: {type: [string, 'null'], format: date-time}

    FavoriteState:
      type: object
      required: [favorite_count, favorited]
      properties:
        favorite_count: {type: integer, minimum: 0}
        favorited: {type: boolean}

    GalleryPage:
      type: object
      required: [items, next_cursor, has_more]
      properties:
        items:
          type: array
          items: {$ref: '#/components/schemas/GalleryItem'}
        next_cursor: {type: [string, 'null']}
        has_more: {type: boolean}

    RoomRealtimeEvent:
      type: object
      required: [id, type, created_at]
      properties:
        id: {type: integer, format: int64}
        type:
          type: string
          enum: [media_ready, media_updated, media_deleted, favorite_changed, room_updated, members_changed]
        entity_id: {type: string, format: uuid}
        created_at: {type: string, format: date-time}

    ProblemReportContext:
      type: object
      additionalProperties: false
      properties:
        route: {type: string, maxLength: 160, description: Server-sanitized route without room codes, invitation tokens, or query parameters}
        app_build: {type: string, maxLength: 80}
        platform: {type: string, maxLength: 120}
        browser: {type: string, maxLength: 500}
        telegram: {type: boolean}
        online: {type: boolean}
        error_code: {type: string, maxLength: 80}
        request_id: {type: string, maxLength: 100}
        occurred_at: {type: string, format: date-time}

    ProblemReport:
      type: object
      required: [id, public_id, category, description, technical_context, status, created_at, updated_at]
      properties:
        id: {type: string, format: uuid}
        public_id: {type: string, pattern: '^ZHY-[A-Z2-7]{8}$'}
        category: {type: string, enum: [upload, download, room, telegram, other]}
        description: {type: string, minLength: 10, maxLength: 2000}
        contact: {type: string, maxLength: 160}
        technical_context: {$ref: '#/components/schemas/ProblemReportContext'}
        status: {type: string, enum: [new, in_progress, resolved, closed]}
        admin_note: {type: string, maxLength: 2000}
        reporter_name: {type: string}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
        resolved_at: {type: string, format: date-time}

    AdminStats:
      type: object
      required: [active_rooms, ready_media, stored_bytes, total_users, new_reports, reports_today, uploads_today, new_users_today]
      properties:
        active_rooms: {type: integer, minimum: 0}
        ready_media: {type: integer, minimum: 0}
        stored_bytes: {type: integer, format: int64, minimum: 0}
        total_users: {type: integer, minimum: 0}
        new_reports: {type: integer, minimum: 0}
        reports_today: {type: integer, minimum: 0}
        uploads_today: {type: integer, minimum: 0}
        new_users_today: {type: integer, minimum: 0}

    Download:
      type: object
      required: [url, expires_at, filename]
      properties:
        url: {type: string, format: uri}
        expires_at: {type: string, format: date-time}
        filename: {type: string}

security:
  - bearerAuth: []
